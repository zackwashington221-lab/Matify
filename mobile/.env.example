# Required for a physical device or a deployed API. Keep the /api suffix.
# Default deployed API. Override only when developing against another API.
EXPO_PUBLIC_API_URL=https://matify.up.railway.app/api
