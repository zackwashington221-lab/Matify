# Matify

This repository contains the Matify applications:

- `web/` — the TanStack Start web application
- `mobile/` — the React Native mobile application
- `server/` — the API server

## Web development

```sh
cd web
npm install
npm run dev
```
